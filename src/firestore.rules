
rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {

    // Helper functions
    function isSignedIn() {
      return request.auth != null;
    }

    function isSuperAdmin() {
      return isSignedIn() && request.auth.token.role == 'Super Admin';
    }

    function getUserData(userId) {
      return get(/databases/$(database)/documents/users/$(userId)).data;
    }
    
    function getRequestingUserData() {
        return getUserData(request.auth.uid);
    }

    function getPermissions() {
      return get(/databases/$(database)/documents/permissions/roles).data;
    }
    
    function hasPermission(role, permission) {
      return getPermissions()[role][permission] == true;
    }

    function isRole(role) {
      return isSignedIn() && request.auth.token.role == role;
    }

    function isSameCompanyAsRequestingUser(entityCompanyId) {
        return getRequestingUserData().companyId == entityCompanyId;
    }
    
    function isAssignee(taskId) {
        return request.auth.uid in get(/databases/$(database)/documents/tasks/$(taskId)).data.assigneeIds;
    }
    
    match /permissions/{docId} {
      allow read, write: if isSuperAdmin();
    }

    match /users/{userId} {
      allow read: if isSuperAdmin() || 
                     (isRole('Manager') && isSameCompanyAsRequestingUser(resource.data.companyId)) ||
                     request.auth.uid == userId;
      
      allow update: if isSuperAdmin() || 
                       (isRole('Manager') && isSameCompanyAsRequestingUser(resource.data.companyId) && hasPermission('Manager', 'canManageUsers')) ||
                       request.auth.uid == userId;

      allow create: if isSuperAdmin() || (isRole('Manager') && hasPermission('Manager', 'canManageUsers'));
      allow delete: if isSuperAdmin() || (isRole('Manager') && hasPermission('Manager', 'canDeleteUsers'));
      
      match /notifications/{notificationId} {
        allow read, update, delete: if isSignedIn() && request.auth.uid == userId;
        allow create: if isSignedIn();
      }
    }

    match /companies/{companyId} {
      allow read: if isSignedIn() && isSameCompanyAsRequestingUser(companyId);
      allow write: if isSuperAdmin() && isSameCompanyAsRequestingUser(companyId);
    }

    match /tasks/{taskId} {
      allow read: if isSuperAdmin() ||
                     (isRole('Manager') && isSameCompanyAsRequestingUser(resource.data.companyId)) ||
                     (isRole('Employee') && isAssignee(taskId)) ||
                     (isRole('Client') && isSameCompanyAsRequestingUser(resource.data.companyId) && hasPermission('Client', 'canViewAssignedTasks'));
                     
      allow create: if isSuperAdmin() ||
                       (isRole('Manager') && hasPermission('Manager', 'canCreateTasks')) ||
                       (isRole('Employee') && hasPermission('Employee', 'canCreateTasks'));

      allow update: if 
          // Admins/Managers can always update
          (isSuperAdmin() || (isRole('Manager') && isSameCompanyAsRequestingUser(resource.data.companyId))) ||
          // Employees can update if they are an assignee AND the current status isn't "Done"
          (isRole('Employee') && isAssignee(taskId) && resource.data.status != 'Done');
      
      allow delete: if isSuperAdmin() || (isRole('Manager') && hasPermission('Manager', 'canDeleteTasks'));

      match /timeTracking/{timeTrackingId} {
          allow read, create, update, delete: if isSuperAdmin() || 
                               (isRole('Manager') && isSameCompanyAsRequestingUser(get(/databases/$(database)/documents/tasks/$(taskId)).data.companyId)) ||
                               (isRole('Employee') && isAssignee(taskId) && hasPermission('Employee', 'canTrackTime'));
      }
      
      match /activities/{activityId} {
        allow read: if isSignedIn() && (
            isSuperAdmin() ||
            (isRole('Manager') && isSameCompanyAsRequestingUser(get(/databases/$(database)/documents/tasks/$(taskId)).data.companyId)) ||
            (isAssignee(taskId))
        );
        allow create: if isSignedIn() && (
             isSuperAdmin() || 
             (isRole('Manager') && isSameCompanyAsRequestingUser(get(/databases/$(database)/documents/tasks/$(taskId)).data.companyId)) ||
             (isRole('Employee') && isAssignee(taskId))
        );
        allow update, delete: if false;
      }
    }
    
    match /brands/{brandId} {
        allow read: if isSignedIn();
        allow write: if isSuperAdmin() || isRole('Manager');
    }

    match /statuses/{statusId} {
        allow read: if isSignedIn();
        allow write: if isSuperAdmin();
    }
    
    match /recurringTaskTemplates/{templateId} {
        allow read, write: if isSuperAdmin() || (isRole('Manager') && isSameCompanyAsRequestingUser(resource.data.companyId));
    }
    
    match /dailyReports/{reportId} {
        allow read: if isSuperAdmin() || (isRole('Manager') && isSameCompanyAsRequestingUser(resource.data.companyId));
        allow write: if isRole('Employee') && getRequestingUserData().companyId == resource.data.companyId;
    }
    
    match /navigationItems/{navItemId} {
        allow read: if isSignedIn();
        allow write: if isSuperAdmin();
    }

    match /socialMediaPosts/{postId} {
        allow read: if isSuperAdmin() || 
                     (isRole('Manager') && isSameCompanyAsRequestingUser(resource.data.companyId));
        allow create: if isSignedIn();
        allow update, delete: if isSuperAdmin() || 
                                 (isRole('Manager') && isSameCompanyAsRequestingUser(resource.data.companyId)) ||
                                 (isRole('Employee') && request.auth.uid == resource.data.createdBy && resource.data.status == 'Draft');
    }
  }
}
