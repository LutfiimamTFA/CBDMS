// This file is now a redirect or can be removed, but for now we'll just have it empty to avoid breaking navigation.
// The new structure uses /admin/settings/workflow/[workstream]
// Keeping it simple and just having a placeholder.

export default function DeprecatedWorkflowPage() {
    return (
        <div className="p-4">
            <p>Please select a workflow from the sidebar menu under "Workflows".</p>
        </div>
    )
}
