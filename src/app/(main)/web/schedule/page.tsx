'use client';
import { SchedulePage } from '@/components/schedule/schedule-page';

export default function WebSchedulePage() {
  // This will be empty for now as webArticles collection doesn't exist yet
  return <SchedulePage workstream="webArticles" />;
}